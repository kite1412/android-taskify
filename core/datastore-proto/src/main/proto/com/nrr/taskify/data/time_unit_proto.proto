syntax = "proto3";

option java_package = "com.nrr.datastore";
option java_multiple_files = true;

enum TimeUnitProto {
    TIME_UNIT_PROTO_MINUTES = 0;
    TIME_UNIT_PROTO_HOURS = 1;
    TIME_UNIT_PROTO_DAYS = 2;
}